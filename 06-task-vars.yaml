- name: play level variables
  hosts: local
  connection: local
  vars:
   course: "DevOps ansible"
   duration: "8.23 HR"
   trainer: sk"
  tasks:
  - name: print course infromation
    vars:
      course: "Ansible"
      duration: "2.2 HR"
      trainer: "ethan hunt"
    ansible.builtin.debug:
      msg: "course is trained by trainer {{ trainer }} for duartion of  {{ duration }} in subject {{ course }}"

  - name: print course infromation
    vars:
      course: "rouge nation"
      duration: "3.5 HR"
      trainer: "dimitri"
    ansible.builtin.debug:
      msg: "course is trained by trainer {{ trainer }} for duartion of  {{ duration }} in subject {{ course }}"
  
  - name: print course infromation
    ansible.builtin.debug:
      msg: "course is trained by trainer {{ trainer }} for duartion of  {{ duration }} in subject {{ course }}"
